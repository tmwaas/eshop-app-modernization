# Terraform local cache and binaries
.terraform/
*.terraform*
.terraform.lock.hcl

# Terraform state files (Sangat rahasia, jangan di-push ke public repo)
*.tfstate
*.tfstate.*

# Crash logs and sensitive variables
*.tfvars
*.tfvars.json
*.log
